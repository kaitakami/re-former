<div class="container">
  <table>
  <tr>
    <th>Username</th>
    <th>Email</th>
  </tr>
  <% @all_users.each do |user| %>
    <tr>
      <td><%= link_to user.username, user_path(user) %></td>
      <td><%= user.email %></td>
    </tr>
  <% end %>
  </table>
</div>
